apiVersion: v1
kind: Secret
metadata:
  name: certificate-api-secrets
  namespace: certificate-platform
type: Opaque
stringData:
  # Placeholder only. Production should use External Secrets, Vault Agent, or CSI projection.
  DATABASE_URL: "postgres://certificate_api:3010@postgres:5432/certificates"
  VAULT_TOKEN: "replace-with-projected-token"
